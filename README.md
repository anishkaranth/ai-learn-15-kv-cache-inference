# ai-learn-15-kv-cache-inference
AI Learn 15: KV-cache inference from scratch in NumPy - mini decoder-only transformer, generation with vs without cache (identical outputs), speedup vs length, memory table
